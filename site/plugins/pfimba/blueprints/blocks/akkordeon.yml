type: akkordeon
title: Akkordeon
label: "{{ titel }}"
preview: fields
wysiwyg: true

fields:
  titel:
    label: Titel
    type: text
    required: true

  offenStandard:
    label: Standardmässig geöffnet
    type: toggle

  inhalt:
    label: Inhalt
    type: blocks
    pretty: true
    fieldsets:
      - aktivitaet
      - leiter
      - anlass
      - google_fotos
      - beitrag
      - quartalsprogramm
      - tabelle
      - download_button
      - bild
